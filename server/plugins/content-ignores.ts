// Configure Nuxt Content to ignore files and directories starting with underscores
export default async () => {
  // This plugin runs during server startup to configure content ignores
  // Nuxt Content will automatically ignore files matching these patterns
}
